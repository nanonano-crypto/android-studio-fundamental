<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.juara.android_fundamental">

    <application
        android:allowBackup="true"
        android:icon="@drawable/robot"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OrderDroidCafeActivity"></activity>
        <activity android:name=".ImageClickActivity" />
        <activity android:name=".UbahWarnaActivity" />
        <activity android:name=".DebuggerActivity" />
        <activity android:name=".ImplisitActivity" />
        <activity android:name=".ParsingDataActivity" />
        <activity android:name=".ScrollviewActivity" />
        <activity android:name=".RelativeActivity" />
        <activity android:name=".UiInteractiveActivity" />
        <activity android:name=".HelloWorldActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>